<!-- res/layout/fragment_home.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="20dp"
        android:text="Thêm Giao Dịch"
        android:textAlignment="center"
        android:textColor="@color/purple"
        android:textStyle="bold" />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/input_form_background"
        android:elevation="10dp"
        android:orientation="vertical"
        android:outlineSpotShadowColor="@color/strong_blue"
        android:padding="8dp">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="14dp"
            android:background="@color/light2_blue_gra"
            android:textStyle="bold"
            app:tabBackground="@drawable/tab_background_selector"
            app:tabIndicatorHeight="0dp"
            app:tabRippleColor="@color/light2_blue_gra"
            app:tabSelectedTextColor="@color/black"
            app:tabTextColor="@color/black">


        </com.google.android.material.tabs.TabLayout>

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/vpTransaction"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/viewpaper_bg"
            android:paddingHorizontal="4dp" />
    </LinearLayout>

</LinearLayout>
